<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.github.simonpercic.aircycle"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.a1.FirstActivity"
            android:label="@string/first_activity" />

        <activity
            android:name=".activity.a2.SecondActivity"
            android:label="@string/second_activity" />

        <activity
            android:name=".activity.a3.ThirdActivity"
            android:label="@string/third_activity" />

        <activity
            android:name=".activity.a4.FourthActivity"
            android:label="@string/fourth_activity" />

        <activity
            android:name=".activity.a5.FifthActivity"
            android:label="@string/fifth_activity" />

    </application>

</manifest>
